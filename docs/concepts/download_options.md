# Download Options

Models are downloaded automatically on first use.

Currently, [Hugging Face](https://huggingface.co/) is the only supported platform and requires the `huggingface` extra installed.
